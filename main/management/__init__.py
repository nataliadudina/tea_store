"""
Custom or console, commands are an auxiliary tool for managing the project state,
data changes, periodic tasks or primary database filling.

You can create a command using the following steps:
    1. In the application, add the management package <management>
    2. Within this package, create a command package <commands>
    3. Inside the commands package, create a file with the name by which the command will be called
"""
